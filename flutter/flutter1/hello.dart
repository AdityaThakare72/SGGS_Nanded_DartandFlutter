import 'package:flutter/material.dart';

/* material app is our rootwidget and will act as a wrapper
 for rest of the widgets inside it


*/
void main() => runApp(MaterialApp(
      // home property will have a text widget and inside we can pass a string
      // different property in widgets so good to place ,

      home: Text('Hello'),
    ));
